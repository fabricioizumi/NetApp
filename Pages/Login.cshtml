@page
@model NetApp.Pages.LoginModel
@{
    Layout = null;
}
<form method="POST">
    <fieldset>
        <legend>Login</legend>
        <span>
            <label>Login:&nbsp</label><input name='login' maxlength="30"/>
        </span>
        <span>
            <label>Password:&nbsp</label><input name='password' type='password' maxlength="15"/>
        </span>
        <input type="submit" value="Send">
    </fieldset>
</form>